import { TextField } from '@mui/material';

const GroupSearch = () => {
	return (
		<TextField
			label="Search by address"
			variant="outlined"
			fullWidth
			size="small"
		/>
	);
};

export default GroupSearch;
